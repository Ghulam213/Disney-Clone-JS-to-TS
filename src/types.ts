export interface Movie {
  id: string;
  backgroundImg: string;
  cardImg: string;
  description: string;
  subTitle: string;
  title: string;
  titleImg: string;
  type: string;
}

export interface User {
  name: string;
  email: string;
  photo: string;
}
